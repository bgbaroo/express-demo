import { newRouterUsers } from "./users";
import { newRouterGroups } from "./groups";
import { newRouterClipboard } from "./clipboards";

export default {
  newRouterUsers,
  newRouterGroups,
  newRouterClipboard,
};
